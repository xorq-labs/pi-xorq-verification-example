# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.coverage

# node / pi package
node_modules/
.pi/git/
.pi/npm/
.pi-subagents/

# local demo catalog + build artifacts (duel.sh recreates these)
.xorq/
builds/

# per-machine trial artifacts (bench/trial_runs.py)
bench/trials/

# raw recordings (record.sh) — only the rendered README gif is tracked
*.cast
*.rec

# nix / direnv
result
result-*
.direnv/

# editor / os
.DS_Store
